{
  "scripts": {
    "fmt": "echo 'BIOME' && biome format || true && echo 'PRETTIER' && prettier --no-error-on-unmatched-pattern '**/*.{html,md}' --check",
    "lint": "biome lint",
    "fix": "echo 'BIOME' && biome lint --write && biome format --write && echo '\nPRETTIER' && prettier --no-error-on-unmatched-pattern '**/*.{html,md}' --write",
    "watch": "webpack --watch",
    "build": "webpack"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.3",
    "copy-webpack-plugin": "^12.0.2",
    "lefthook": "^1.7.18",
    "prettier": "^3.3.3",
    "webextension-polyfill": "^0.12.0",
    "webpack": "^5.95.0",
    "webpack-cli": "^5.1.4"
  }
}
